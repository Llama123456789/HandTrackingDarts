using System.Collections;
using System.Collections.Generic;
using TMPro;
using UnityEngine;

public class DartGameMode : MonoBehaviour
{
    /*public static int[] gameMode = {0,1,2};
    public static int dartGameModeNum;
    [SerializeField]
    private TextMeshProUGUI dartsScore;
    public static void ScoreManager(int score)
    {
        Debug.Log("スコアマネージャー");
        int scoreCal = ScoreCalculation(score);
    }
    public static int ScoreCalculation(int score)
    {
        int scoreResult = 0;
        if (gameMode[0] == dartGameModeNum)
        return 
    }*/
}
